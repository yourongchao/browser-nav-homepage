## 优化方案

1. **修改布局设置逻辑**：将固定列数设置改为基于最小列宽的自适应布局
2. **保留用户交互**：仍允许用户调整列数，但转换为调整最小列宽
3. **优化CSS Grid配置**：使用`auto-fill`确保元素自动换行
4. **统一布局设置**：确保所有相关函数使用相同的布局逻辑

## 具体实现步骤

1. **修改`updateLayout`函数**（script.js:1853）：
   - 将固定列数设置改为`auto-fill`自适应布局
   - 基于用户设置的列数计算合适的最小列宽

2. **修正工具组渲染逻辑**（script.js:2094）：
   - 统一使用与主布局相同的自适应逻辑
   - 确保工具组项与普通快捷方式保持一致的布局

3. **优化CSS样式**：
   - 确保`nav-grid`类的CSS定义与JavaScript动态设置兼容
   - 添加适当的媒体查询，优化不同屏幕尺寸下的显示效果

4. **测试验证**：
   - 测试不同屏幕尺寸下的布局表现
   - 测试列数滑块调整时的响应效果
   - 确保快捷方式和工具组都能正确自适应

## 预期效果

- 当视窗宽度减小时，快捷方式会自动换行显示
- 用户仍可通过滑块调整布局密度，影响每行显示的快捷方式数量
- 布局过渡自然，元素间距均匀
- 兼容不同屏幕尺寸，提供良好的视觉体验